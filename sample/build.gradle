buildscript {
    repositories { jcenter() }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.2'
        classpath 'pt.simdea.verifier:verifier:+'
    }
}

allprojects {
    repositories { jcenter() }
}
