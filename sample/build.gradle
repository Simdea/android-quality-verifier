buildscript {
    repositories {
        jcenter()
        flatDir dirs: "../plugin/build/libs"
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.2'
        classpath 'org.stoyicker.android-check:plugin:3.167'
    }
}

allprojects {
    repositories { jcenter() }
}
